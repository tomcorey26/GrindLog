// src/components/RoutineSessionSummary.tsx (TEMPORARY STUB — Task 40 replaces this)
'use client';
import type { RoutineSessionSummary as Summary } from '@/lib/types';

type Props = {
  summary: Summary;
  onDiscard: () => void;
  onSaved: () => void;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function RoutineSessionSummary({ summary, onDiscard, onSaved }: Props) {
  return null;
}
